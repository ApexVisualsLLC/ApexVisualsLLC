# Copy to .env.local and fill in real values. Never commit .env.local.

# Postgres connection string. Auto-injected once the Vercel Postgres
# integration is connected in the Vercel dashboard (pull locally with
# `vercel env pull .env.local`). Code checks DATABASE_URL, POSTGRES_URL,
# STORAGE_DATABASE_URL, and STORAGE_POSTGRES_URL, in that order — this
# project's actual variable ended up prefixed as STORAGE_DATABASE_URL.
DATABASE_URL=
# STORAGE_DATABASE_URL=

# Resend (transactional email) — create an account at resend.com, get an API
# key, and a verified "from" address (their sandbox address works for local
# testing before a sending domain is verified).
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# Admin login for /admin — pick a long, random password. Not the same secret
# as the session signing key below.
ADMIN_PASSWORD=

# Signs the admin session cookie. Generate with: openssl rand -base64 32
ADMIN_SESSION_SECRET=

# Daily backup of the bookings table to a Google Sheet (see vercel.json for
# the cron schedule). GOOGLE_SHEETS_CLIENT_EMAIL and _PRIVATE_KEY come from a
# Google Cloud service account JSON key (Sheets API only, no other project
# roles needed) — share the target spreadsheet with that service account's
# email as an Editor. The spreadsheet must have a tab named exactly
# "Bookings". GOOGLE_SHEETS_SPREADSHEET_ID is the id from the sheet's URL
# (not secret). CRON_SECRET is generated with: openssl rand -base64 24 —
# Vercel sends it automatically as a Bearer token when calling the cron
# route, so only Vercel's own scheduler can trigger a backup.
GOOGLE_SHEETS_CLIENT_EMAIL=
GOOGLE_SHEETS_PRIVATE_KEY=
GOOGLE_SHEETS_SPREADSHEET_ID=
CRON_SECRET=

# Stripe — deposit payment via a hosted Checkout Session (no Stripe.js/card
# data ever touches our server or browser). STRIPE_SECRET_KEY is the API
# secret key from the Stripe Dashboard (start with a test-mode key —
# sk_test_...). STRIPE_WEBHOOK_SECRET verifies that /api/webhooks/stripe
# requests genuinely come from Stripe; get it by registering that URL as a
# webhook endpoint in the Dashboard (production) or from `stripe listen`
# (local dev) — these are two DIFFERENT secrets, don't mix them up.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Real availability data, replacing the placeholder schedule
# (src/lib/availability/mock-source.ts). Reuses the same service account as
# the Sheets backup above (GOOGLE_SHEETS_CLIENT_EMAIL / _PRIVATE_KEY) — just
# share a dedicated "Apex Visuals Bookings" Google Calendar with that
# service account's email and set this to that calendar's ID (Calendar
# settings -> "Integrate calendar" -> Calendar ID; looks like an email ending
# in @group.calendar.google.com). Leave unset for local dev — the placeholder
# schedule is used automatically instead. Sharing permission must be "Make
# changes to events" (not just "See only free/busy") — once a deposit is
# paid, the app creates a real event on this calendar to block the slot.
GOOGLE_CALENDAR_ID=

# Optional: comma-separated Google Calendar IDs (beyond GOOGLE_CALENDAR_ID
# above) to also treat as "Russell is busy" — e.g. his personal calendar
# and/or a school schedule calendar. Each one must be shared with the same
# service account, "See only free/busy" is enough for these. Personal
# calendar ID is usually just the Gmail address; a school calendar's ID is
# in that calendar's settings -> "Integrate calendar" -> Calendar ID. Leave
# unset to only check the booking calendar itself (the old behavior).
GOOGLE_AVAILABILITY_CALENDAR_IDS=

# Cloudflare R2 (photo/video storage for the watermarked-preview + final-
# delivery pipeline). Create an R2 bucket in the Cloudflare dashboard, then
# an R2 API token scoped to just that bucket — gives you R2_ACCOUNT_ID,
# R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY. R2_BUCKET_NAME is the bucket's
# name (not secret). The bucket must stay fully private — no public access —
# every read/write goes through a presigned URL generated server-side.
# Configure the bucket's CORS policy in the Cloudflare dashboard to allow
# PUT/GET/HEAD from https://apexvisualsutah.com so the admin upload page can
# PUT files directly to R2 from the browser:
#   [{"AllowedOrigins": ["https://apexvisualsutah.com"], "AllowedMethods":
#     ["PUT","GET","HEAD"], "AllowedHeaders": ["*"], "MaxAgeSeconds": 3600}]
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
